{
  "private": true,
  "workspaces": [
    "api",
    "client"
  ],
  "scripts": {
    "start": "yarn workspace api start",
    "lint": "eslint api & eslint client",
    "build": "yarn workspace api build && yarn workspace client build"
  }
}
